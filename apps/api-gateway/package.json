{
  "name": "@marketmind/api-gateway",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@marketmind/events": "workspace:*",
    "@marketmind/types": "workspace:*"
  },
  "devDependencies": {
    "@marketmind/config": "workspace:*",
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "~5.5.0"
  }
}
